language: node_js
node_js:
  - "8"
dist: trusty
sudo: false

addons:
  chrome: stable

before_script:
  - npm install -g @angular/cli

script:
  - cd dagger-app
  - ls -ll
  - ng build --prod --aot
  - ng test

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $sh72hiw82hela823hrkjn29
  local_dir: dist
  on:
    branch: master
